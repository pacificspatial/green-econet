{
    "name": "enp-aws-infrastructure",
    "version": "1.0.0",
    "description": "AWS infrastructure for Econet Plateau",
    "scripts": {
        "stg:deploy": "serverless deploy --stage stg --region ap-northeast-1 --verbose --aws-profile enp",
        "stg:destroy": "echo serverless remove --verbose --stage stg --region ap-northeast-1 --aws-profile enp"
    },
    "author": "econetplateau",
    "license": "UNLICENSED",
    "devDependencies": {
    "serverless": "^2.68.0"
  }
}
